# Google API Key — used client-side for Maps JavaScript, Places, and Geocoding APIs
GOOGLE_API_KEY=your_google_api_key_here

# Server port
PORT=3000

# Service account key file for Solar API (server-side authentication)
# Place the JSON key file at: config/service-account.json
# Download from: Google Cloud Console > IAM & Admin > Service Accounts > Keys
